// Stub for Stage 1 — WebGPU initialization will be implemented later.
export async function initWebGPU(): Promise<void> {
  // Intentionally empty for Stage 0 to avoid TypeScript errors when imported.
  return;
}
